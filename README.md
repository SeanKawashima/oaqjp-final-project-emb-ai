# Emotion Detection Web Application
Final project for IBM AI Engineering course using Watson NLP and Flask.
# Create the directory
mkdir final_project

# Clone YOUR forked repository into the directory
git clone <PASTE-YOUR-FORK-URL-HERE> final_project

# Move into the project folder
cd final_project
# Create the directory
mkdir final_project

# Clone YOUR forked repository into the directory
git clone <PASTE-YOUR-FORK-URL-HERE> final_project

# Move into the project folder
cd final_project
git add README.md
git commit -m "Add project name to README"
git push origin main
